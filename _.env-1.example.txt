# ── Bot Configuration ──────────────────────────────────────────────────────────
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321          # Comma-separated Telegram user IDs
PAYMENT_LOG_CHANNEL=-100xxxxxxxxxx     # Channel ID where approved withdrawals are logged

# ── Mini App ───────────────────────────────────────────────────────────────────
WEBAPP_URL=https://your-railway-app.up.railway.app   # Public URL of this deployment
MINI_APP_SECRET=change_this_to_a_random_secret_string

# ── VPN/Proxy Check ────────────────────────────────────────────────────────────
# Free tier: https://proxycheck.io  (no key needed, 100 req/day free)
PROXYCHECK_API_KEY=                    # Optional – for higher limits

# ── Database ───────────────────────────────────────────────────────────────────
# Leave blank to use SQLite (default). Set a postgres:// URL for PostgreSQL.
DATABASE_URL=

# ── Defaults ───────────────────────────────────────────────────────────────────
DEFAULT_REWARD=10                      # Default coins per verified referral
MIN_WITHDRAWAL=50                      # Minimum withdrawal amount (set by admin)
